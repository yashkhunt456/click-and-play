<%= form_with model: [@boxhouse, @box], local: true do |form| %>
  <div>
    <%= form.label :name, "Name : " %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit "Create Box", class: "btn btn-sm btn-primary mt-2" %>
  </div>
<% end %>